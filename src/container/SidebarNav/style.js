import styled from 'styled-components';

export const SideNavbar = styled.div`
  padding: var(--gap15);
  ul {
    padding: var(--gap0);
    margin: var(--gap0);
    li {
      text-align: center;
      list-style: none;
    }
  }
`;
